<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Users List</title>
  <link rel="stylesheet" href="/styles.css" />
  <style>
    .btn-edit {
      background-color: #17a2b8;
      color: white;
      padding: 6px 12px;
      border-radius: 4px;
      text-decoration: none;
      font-size: 14px;
      margin-right: 6px;
      display: inline-block;
    }
    .btn-edit:hover {
      background-color: #117a8b;
    }
    .btn-delete {
      background-color: #dc3545;
      border: none;
      color: white;
      padding: 6px 12px;
      border-radius: 4px;
      cursor: pointer;
      font-size: 14px;
      transition: background-color 0.3s ease;
      display: inline-block;
    }
    .btn-delete:hover {
      background-color: #c82333;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }
    th, td {
      padding: 12px;
      border: 1px solid #ddd;
      text-align: left;
    }
    th {
      background-color: #007bff;
      color: white;
    }
    tr:nth-child(even) {
      background-color: #f2f2f2;
    }
  </style>
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container">
    <h1>Users List</h1>
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>Name</th>
          <th>Email</th>
          <th>จัดการ</th>
        </tr>
      </thead>
      <tbody>
        <% users.forEach(user => { %>
          <tr>
            <td><%= user.id %></td>
            <td><%= user.name %></td>
            <td><%= user.email %></td>
            <td>
              <a href="/users/edit/<%= user.id %>" class="btn-edit">แก้ไข</a>
              <form action="/users/delete/<%= user.id %>" method="POST" onsubmit="return confirm('ยืนยันลบผู้ใช้นี้หรือไม่?');" style="display:inline;">
                <button type="submit" class="btn-delete">ลบ</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>
