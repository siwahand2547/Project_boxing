<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>เปรียบเทียบนักชก</title>
  <script src="/socket.io/socket.io.js"></script>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #121212, #1a1a1a);
      color: #fff;
      margin: 0;
      padding: 20px;
      overflow-x: hidden;
    }
    h1, h2 {
      color: #fff;
      border-left: 5px solid #007bff;
      padding-left: 15px;
      margin-bottom: 20px;
    }
    .container {
      max-width: 1200px;
      margin: 0 auto;
    }
    table.fighter-info, table.realtime-table, table.fight-rounds {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
      background: #1e1e1e;
      box-shadow: 0 4px 8px rgba(0,0,0,0.5);
      border-radius: 8px;
      overflow: hidden;
    }
    table.fighter-info th, table.realtime-table th, table.fight-rounds th {
      background: #333;
      color: #fff;
      padding: 12px;
      text-align: center;
      font-weight: bold;
    }
    table.fighter-info td, table.realtime-table td, table.fight-rounds td {
      padding: 12px;
      text-align: center;
      border-top: 1px solid #444;
    }
    .fighter-info-container {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      margin-bottom: 20px;
    }
    .fighter-info {
      flex: 1 1 45%;
      min-width: 300px;
    }
    img {
      width: 100px;
      height: 100px;
      object-fit: cover;
      border-radius: 6px;
      box-shadow: 0 0 8px rgba(255,255,255,0.2);
    }
    .realtime-container {
      flex: 1 1 45%;
      min-width: 300px;
      background: #181818;
      border: 2px solid #007bff;
      border-radius: 10px;
      padding: 15px;
      box-sizing: border-box;
      max-height: 300px;
      overflow-y: auto;
      margin-bottom: 20px;
    }
    .realtime-container.fighter2 {
      border-color: #6c757d;
    }
    .realtime-container h2 {
      color: #fff;
      margin: 0 0 10px 0;
      font-size: 1.2em;
    }
    .clear {
      clear: both;
      margin-top: 30px;
    }
    .watch-btn {
      background: #007bff;
      color: #fff;
      border: none;
      padding: 8px 16px;
      border-radius: 6px;
      cursor: pointer;
      transition: background 0.3s ease, transform 0.2s;
    }
    .watch-btn:hover {
      background: #0056b3;
      transform: scale(1.05);
    }
    button {
      font-size: 14px;
      margin: 5px;
      border: none;
      background: #444;
      color: #fff;
      padding: 10px 16px;
      border-radius: 6px;
      cursor: pointer;
      transition: background 0.2s ease;
    }
    button:hover {
      background: #666;
    }
    button:disabled {
      background: #555;
      cursor: not-allowed;
      opacity: 0.6;
    }
    #clipModal, #videoModal, #summaryModal {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0,0,0,0.9);
      z-index: 1000;
      justify-content: center;
      align-items: center;
      flex-direction: column;
    }
    #clipModal .modal-content, #videoModal .modal-content, #summaryModal .modal-content {
      background: #1e1e1e;
      padding: 20px;
      border-radius: 8px;
      max-width: 90%;
      max-height: 90vh;
      overflow: auto;
      position: relative;
    }
    #clipModal video {
      max-width: 45%;
      max-height: 70vh;
      border-radius: 8px;
      background: #000;
      box-shadow: 0 0 15px rgba(0,0,0,0.7);
      margin: 10px;
    }
    #videoModal video {
      max-width: 800px;
      max-height: 80vh;
      border-radius: 8px;
    }
    .modal-close {
      position: absolute;
      top: 15px;
      right: 15px;
      color: #fff;
      font-size: 28px;
      cursor: pointer;
      transition: color 0.2s;
    }
    .modal-close:hover {
      color: #007bff;
    }
    #summaryModal .modal-content {
      max-width: 500px;
      width: 90%;
    }
    #summaryContent {
      max-height: 60vh;
      overflow-y: auto;
      padding: 15px;
      border: 1px solid #444;
      border-radius: 6px;
      background: #222;
    }
    .connected { color: #00ff00; font-weight: bold; }
    .disconnected { color: #ff4444; font-weight: bold; }
    @media (max-width: 768px) {
      .fighter-info, .realtime-container {
        flex: 1 1 100%;
      }
      #clipModal video {
        max-width: 90%;
      }
    }

    #cameraTestModal {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0,0,0,0.9);
      z-index: 1000;
      justify-content: center;
      align-items: center;
      flex-direction: column;
    }
    #cameraTestModal .modal-content {
      background: #1e1e1e;
      padding: 20px;
      border-radius: 8px;
      max-width: 90%;
      max-height: 90vh;
      overflow: auto;
      position: relative;
    }
    #cameraTestModal video {
      max-width: 45%;
      max-height: 70vh;
      border-radius: 8px;
      background: #000;
      box-shadow: 0 0 15px rgba(0,0,0,0.7);
      margin: 10px;
      }
  </style>
</head>
<body>
  <div class="container">
    <!-- Modal สำหรับทดสอบกล้อง -->
    <div id="cameraTestModal">
      <div class="modal-content">
        <span class="modal-close" onclick="closeCameraTestModal()">&times;</span>
        <h2>ทดสอบกล้อง</h2>
        <div style="display: flex; gap: 15px; justify-content: center; flex-wrap: wrap;">
          <video id="testCam1" autoplay playsinline style="display: none;"></video>
          <video id="testCam2" autoplay playsinline style="display: none;"></video>
        </div>
        <button onclick="closeCameraTestModal()">ปิด</button>
      </div>
    </div>

    <!-- Modal สำหรับแสดงวิดีโอ 2 มุม -->
    <div id="clipModal">
      <div class="modal-content">
        <span class="modal-close" onclick="closeClipModal()">&times;</span>
        <div style="display: flex; gap: 15px; justify-content: center; flex-wrap: wrap;">
          <video id="clipVideo1" muted preload="auto" controls style="display: none;"></video>
          <video id="clipVideo2" muted preload="auto" controls style="display: none;"></video>
        </div>
      </div>
    </div>

    <!-- Modal สำหรับสรุปผล -->
    <div id="summaryModal">
      <div class="modal-content">
        <span class="modal-close" onclick="document.getElementById('summaryModal').style.display='none'">&times;</span>
        <h2>สรุปผลการแข่งขัน</h2>
        <div id="summaryContent">กำลังโหลดข้อมูล...</div>
        <button onclick="document.getElementById('summaryModal').style.display='none'" style="align-self: flex-end;">ปิด</button>
      </div>
    </div>

    <%- include('partials/navbar') %>
    <h1>เปรียบเทียบนักชก (ID: <%= schedulefightId %>)</h1>

    <!-- ข้อมูลนักชก -->
    <div class="fighter-info-container">
      <div class="fighter-info">
        <table class="fighter-info">
          <thead>
            <tr><th>ชื่อ</th><th>ค่าย</th><th>รุ่นน้ำหนัก</th><th>ภาพ</th></tr>
          </thead>
          <tbody>
            <tr>
              <td><%= fighter1.name %></td>
              <td><%= fighter1.camp %></td>
              <td><%= fighter1.weight_class %></td>
              <td><img src="<%= fighter1.photo || '/uploads/default.jpg' %>" alt="fighter1"></td>
            </tr>
          </tbody>
        </table>
      </div>
      <div class="fighter-info">
        <table class="fighter-info">
          <thead>
            <tr><th>ชื่อ</th><th>ค่าย</th><th>รุ่นน้ำหนัก</th><th>ภาพ</th></tr>
          </thead>
          <tbody>
            <tr>
              <td><%= fighter2.name %></td>
              <td><%= fighter2.camp %></td>
              <td><%= fighter2.weight_class %></td>
              <td><img src="<%= fighter2.photo || '/uploads/default.jpg' %>" alt="fighter2"></td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>

    <!-- สถานะพอร์ต -->
    <p>
      สถานะพอร์ต COM:<br />
      ➤ <span id="fighter1-port">กำลังโหลด...</span>: <span id="fighter1-status" class="disconnected">ยังไม่เชื่อมต่อ</span><br />
      ➤ <span id="fighter2-port">กำลังโหลด...</span>: <span id="fighter2-status" class="disconnected">ยังไม่เชื่อมต่อ</span>
    </p>

    <!-- ข้อมูลเรียลไทม์ -->
    <div class="fighter-info-container">
      <div class="realtime-container" id="realtime1">
        <h2 id="fighter1-header">ข้อมูล Real-time นักชก 1 (กำลังโหลด...)</h2>
        <table class="realtime-table" id="fighter1Table">
          <thead>
            <tr><th>เวลา</th><th>ตำแหน่ง</th><th>ข้อมูล (N)</th></tr>
          </thead>
          <tbody></tbody>
        </table>
      </div>
      <div class="realtime-container fighter2" id="realtime2">
        <h2 id="fighter2-header">ข้อมูล Real-time นักชก 2 (กำลังโหลด...)</h2>
        <table class="realtime-table" id="fighter2Table">
          <thead>
            <tr><th>เวลา</th><th>ตำแหน่ง</th><th>ข้อมูล (N)</th></tr>
          </thead>
          <tbody></tbody>
        </table>
      </div>
    </div>

    <!-- ปุ่มควบคุมการบันทึกและสลับพอร์ต -->
    <div style="text-align: center; margin: 20px 0;">
      <button onclick="startRecording()">🎥 เริ่มการแข่งขัน</button>
      <button onclick="stopRecording()" id="stopBtn" disabled>⛔ จบการแข่งขัน</button>
      <button id="btnSummary">📊 สรุปผลการแข่งขัน</button>
      <button onclick="swapPorts()">🔄 สลับพอร์ตนักชก</button>
      <button onclick="testCameras()">📸 ทดสอบกล้อง</button>
    </div>

    <!-- ข้อมูลย้อนหลัง -->
    <h2>ข้อมูลการแข่งขันย้อนหลัง</h2>
    <div id="fightRoundsContainer">
      <% Object.keys(fightDataGrouped).forEach(round => { %>
        <table class="fight-rounds">
          <thead>
            <tr>
              <th colspan="5">ยกที่ <%= round %></th>
            </tr>
            <tr>
              <th>เวลาที่โดน</th>
              <th>ระยะเวลา</th>
              <th>นักชก</th>
              <th>รายละเอียด</th>
              <th>คลิปกล้อง</th>
            </tr>
          </thead>
          <tbody>
            <% fightDataGrouped[round].forEach(item => { 
              const fighterName = fighterIdNameMap[item.fighterid] || item.fighterid;
            %>
              <tr>
                <td><%= item.timehit %></td>
                <td><%= item.time %></td>
                <td><%= fighterName %></td>
                <td><%= item.fighterdetail %></td>
                <td>
                  <% if (item.clipdetail && item.clipdetail2) { %>
                    <button class="watch-btn" onclick="openClipModal('<%= item.clipdetail %>', '<%= item.clipdetail2 %>', '<%= item.timehit %>')">ดูคลิป 2 มุม</button>
                  <% } else if (item.clipdetail) { %>
                    <button class="watch-btn" onclick="openClipModal('<%= item.clipdetail %>', '', '<%= item.timehit %>')">ดูกล้อง 1</button>
                  <% } else if (item.clipdetail2) { %>
                    <button class="watch-btn" onclick="openClipModal('', '<%= item.clipdetail2 %>', '<%= item.timehit %>')">ดูกล้อง 2</button>
                  <% } else { %>
                    ไม่มีคลิป
                  <% } %>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% }) %>
    </div>

    <!-- กล้องเรียลไทม์ -->
    <div style="display: flex; gap: 15px; justify-content: center; flex-wrap: wrap; margin-top: 20px;">
      <video id="cam1" autoplay playsinline width="320" height="240" style="background: #000; border-radius: 8px;"></video>
      <video id="cam2" autoplay playsinline width="320" height="240" style="background: #000; border-radius: 8px;"></video>
    </div>
  </div>

  <script>
    const socket = io();
    let mediaRecorder1, mediaRecorder2;
    let recordedChunks1 = [], recordedChunks2 = [];
    let startTime = null;
    let realtimeData = [];
    let roundsData = [];
    let roundNumber = <%= typeof roundNumberStart !== 'undefined' ? roundNumberStart : 1 %>;
    let testStream1 = null, testStream2 = null; // สำหรับทดสอบกล้อง

    socket.on('connect', () => {
      console.log('Socket.IO connected');
      socket.emit('requestConnectionStatus');
    });

    // ฟังสถานะการเชื่อมต่อ
    socket.on('connectionStatus', (status) => {
      console.log('Connection status:', status);
      document.getElementById('fighter1-port').textContent = status.port1 || 'ไม่ระบุ';
      document.getElementById('fighter2-port').textContent = status.port2 || 'ไม่ระบุ';

      const status1 = document.getElementById('fighter1-status');
      status1.textContent = status.connected1 ? 'เชื่อมต่อแล้ว' : 'ยังไม่เชื่อมต่อ';
      status1.classList.toggle('connected', status.connected1);
      status1.classList.toggle('disconnected', !status.connected1);

      const status2 = document.getElementById('fighter2-status');
      status2.textContent = status.connected2 ? 'เชื่อมต่อแล้ว' : 'ยังไม่เชื่อมต่อ';
      status2.classList.toggle('connected', status.connected2);
      status2.classList.toggle('disconnected', !status.connected2);

      document.getElementById('fighter1-header').textContent = `ข้อมูล Real-time นักชก 1 (${status.port1 || 'ไม่ระบุ'})`;
      document.getElementById('fighter2-header').textContent = `ข้อมูล Real-time นักชก 2 (${status.port2 || 'ไม่ระบุ'})`;
    });

    // ฟังข้อมูลเรียลไทม์
    socket.on('COM_PORT_1_DATA', (data) => {
      console.log('📥 COM_PORT_1_DATA:', data);
      const currentTimeSec = startTime ? Math.floor((Date.now() - startTime) / 1000) : 0;
      Object.keys(data).forEach(pos => {
        addRealtimeData('นักชก1', pos, data[pos], currentTimeSec);
        addTableRow('fighter1Table', pos, data[pos], currentTimeSec);
      });
    });

    socket.on('COM_PORT_2_DATA', (data) => {
      console.log('📥 COM_PORT_2_DATA:', data);
      const currentTimeSec = startTime ? Math.floor((Date.now() - startTime) / 1000) : 0;
      Object.keys(data).forEach(pos => {
        addRealtimeData('นักชก2', pos, data[pos], currentTimeSec);
        addTableRow('fighter2Table', pos, data[pos], currentTimeSec);
      });
    });

    // ฟังก์ชันสลับพอร์ต
    function swapPorts() {
      socket.emit('swapCOMPorts');
      alert('กำลังสลับพอร์ตนักชก...');
    }

    function addRealtimeData(label, position, value, timeHitSec = 0) {
      console.log(`💾 addRealtimeData: label=${label}, position=${position}, value=${value}, timeHitSec=${timeHitSec}`);
      realtimeData.push({
        label,
        position,
        value: `${value}|${timeHitSec}`,
        fighterdetail: `${label} ${value} ${position}`
      });
    }

    function addTableRow(tableId, position, value, timeSec) {
      const tbody = document.querySelector(`#${tableId} tbody`);
      const tr = document.createElement('tr');
      const elapsedStr = startTime
        ? `${Math.floor(timeSec / 60)}:${(timeSec % 60).toString().padStart(2, '0')}`
        : '0:00';
      tr.innerHTML = `
        <td>${elapsedStr}</td>
        <td>${position}</td>
        <td>${value}</td>
      `;
      tbody.prepend(tr);
      while (tbody.children.length > 20) {
        tbody.removeChild(tbody.lastChild);
      }
      tbody.parentElement.scrollTop = 0;
    }

    function startRecording() {
      startTime = Date.now();
      console.log('🔄 startRecording: startTime set to', startTime);
      recordedChunks1 = [];
      recordedChunks2 = [];
      realtimeData = [];

      navigator.mediaDevices.enumerateDevices()
        .then(devices => {
          const videoInputs = devices.filter(device => device.kind === 'videoinput');
          console.log('📹 Video inputs:', videoInputs);
          const promises = [];
          if (videoInputs.length >= 1) {
            promises.push(navigator.mediaDevices.getUserMedia({ video: { deviceId: videoInputs[0].deviceId }, audio: true }));
          } else {
            console.warn('⚠️ ไม่พบกล้องตัวที่ 1');
          }
          if (videoInputs.length >= 2) {
            promises.push(navigator.mediaDevices.getUserMedia({ video: { deviceId: videoInputs[1].deviceId }, audio: true }));
          } else {
            console.warn('⚠️ ไม่พบกล้องตัวที่ 2');
          }
          return Promise.allSettled(promises);
        })
        .then(results => {
          const cam1 = document.getElementById('cam1');
          const cam2 = document.getElementById('cam2');
          let stream1 = null, stream2 = null;

          if (results[0]?.status === 'fulfilled') {
            stream1 = results[0].value;
            cam1.srcObject = stream1;
            cam1.play().catch(err => console.error('Error playing cam1:', err));
            mediaRecorder1 = new MediaRecorder(stream1);
            mediaRecorder1.ondataavailable = e => {
              if (e.data.size > 0) recordedChunks1.push(e.data);
            };
            mediaRecorder1.start();
          } else {
            console.error('❌ ล้มเหลวในการเปิดกล้องตัวที่ 1:', results[0]?.reason);
            alert('ไม่สามารถเปิดกล้องตัวที่ 1 ได้: ' + results[0]?.reason?.message);
          }

          if (results[1]?.status === 'fulfilled') {
            stream2 = results[1].value;
            cam2.srcObject = stream2;
            cam2.play().catch(err => console.error('Error playing cam2:', err));
            mediaRecorder2 = new MediaRecorder(stream2);
            mediaRecorder2.ondataavailable = e => {
              if (e.data.size > 0) recordedChunks2.push(e.data);
            };
            mediaRecorder2.start();
          } else {
            console.warn('⚠️ ไม่สามารถเปิดกล้องตัวที่ 2 ได้:', results[1]?.reason);
            alert('ไม่สามารถเปิดกล้องตัวที่ 2 ได้: ' + (results[1]?.reason?.message || 'ไม่พบกล้อง'));
          }

          if (stream1 || stream2) {
            document.getElementById('stopBtn').disabled = false;
            alert('เริ่มบันทึกกล้อง' + (stream1 && stream2 ? 'ทั้งสองตัว' : stream1 ? 'ตัวที่ 1' : 'ตัวที่ 2') + 'แล้ว!');
          } else {
            throw new Error('ไม่สามารถเปิดกล้องใดๆ ได้');
          }
        })
        .catch(err => {
          console.error('เกิดข้อผิดพลาด:', err);
          alert('ไม่สามารถเปิดกล้องได้: ' + err.message);
        });
    }

    async function stopRecording() {
      const stopPromises = [];
      let blob1 = null, blob2 = null;
      if (mediaRecorder1?.state !== 'inactive') {
        stopPromises.push(new Promise(resolve => {
          mediaRecorder1.onstop = () => resolve(new Blob(recordedChunks1, { type: 'video/webm' }));
          mediaRecorder1.stop();
        }));
      }
      if (mediaRecorder2?.state !== 'inactive') {
        stopPromises.push(new Promise(resolve => {
          mediaRecorder2.onstop = () => resolve(new Blob(recordedChunks2, { type: 'video/webm' }));
          mediaRecorder2.stop();
        }));
      }

      try {
        const blobs = await Promise.all(stopPromises);
        if (blobs.length > 0 && blobs[0] && blobs[0].size > 0) blob1 = blobs[0];
        if (blobs.length > 1 && blobs[1] && blobs[1].size > 0) blob2 = blobs[blobs.length - 1];

        const uploadPromises = [];
        let clipUrl1 = '', clipUrl2 = '';
        if (blob1) {
          const formData1 = new FormData();
          formData1.append('video', blob1, 'fight_cam1.webm');
          uploadPromises.push(fetch('/upload-video', { method: 'POST', body: formData1 }).then(res => res.json()));
        }
        if (blob2) {
          const formData2 = new FormData();
          formData2.append('video', blob2, 'fight_cam2.webm');
          uploadPromises.push(fetch('/upload-video', { method: 'POST', body: formData2 }).then(res => res.json()));
        }

        const uploadResults = await Promise.all(uploadPromises);
        if (blob1 && uploadResults[0]?.success) clipUrl1 = uploadResults[0].url;
        if (blob2 && uploadResults[1]?.success) clipUrl2 = uploadResults[1].url;

        const endTime = Date.now();
        const durationMs = endTime - startTime;
        const totalSeconds = Math.floor(durationMs / 1000);
        const fightDuration = `${Math.floor(totalSeconds / 60)}:${(totalSeconds % 60).toString().padStart(2, '0')}`;
        const saveRes = await fetch('/datafight/save', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            schedulefight_id: "<%= schedulefightId %>",
            clip_url: clipUrl1,
            clip_url2: clipUrl2,
            time: fightDuration,
            data: realtimeData,
            round: roundNumber
          })
        });
        const saveData = await saveRes.json();
        if (!saveData.success) throw new Error('Save to DB failed');
        roundsData.push({
          round: roundNumber,
          fightDuration,
          videoUrl: clipUrl1,
          videoUrl2: clipUrl2,
          data: [...realtimeData]
        });
        roundNumber++;
        renderFightRounds();
        alert('บันทึกข้อมูลเรียบร้อยแล้ว' + (clipUrl1 || clipUrl2 ? ' (รวมวิดีโอ)' : ' (ไม่มีวิดีโอ)'));
        document.getElementById('stopBtn').disabled = true;

        // หยุด stream กล้อง
        const cam1 = document.getElementById('cam1');
        const cam2 = document.getElementById('cam2');
        if (cam1.srcObject) {
          cam1.srcObject.getTracks().forEach(track => track.stop());
          cam1.srcObject = null;
        }
        if (cam2.srcObject) {
          cam2.srcObject.getTracks().forEach(track => track.stop());
          cam2.srcObject = null;
        }
      } catch (error) {
        console.error('เกิดข้อผิดพลาด:', error);
        alert('เกิดข้อผิดพลาด: ' + error.message);
      }
    }

    function testCameras() {
      navigator.mediaDevices.enumerateDevices()
        .then(devices => {
          const videoInputs = devices.filter(device => device.kind === 'videoinput');
          console.log('📹 Video inputs for test:', videoInputs);
          const promises = [];
          if (videoInputs.length >= 1) {
            promises.push(navigator.mediaDevices.getUserMedia({ video: { deviceId: videoInputs[0].deviceId }, audio: false }));
          }
          if (videoInputs.length >= 2) {
            promises.push(navigator.mediaDevices.getUserMedia({ video: { deviceId: videoInputs[1].deviceId }, audio: false }));
          }
          return Promise.allSettled(promises);
        })
        .then(results => {
          const modal = document.getElementById('cameraTestModal');
          const testCam1 = document.getElementById('testCam1');
          const testCam2 = document.getElementById('testCam2');

          if (results[0]?.status === 'fulfilled') {
            testStream1 = results[0].value;
            testCam1.srcObject = testStream1;
            testCam1.style.display = 'block';
            testCam1.play().catch(err => console.error('Error playing testCam1:', err));
          } else {
            console.warn('⚠️ ไม่สามารถเปิดกล้องตัวที่ 1 ได้:', results[0]?.reason);
            testCam1.style.display = 'none';
            alert('ไม่สามารถเปิดกล้องตัวที่ 1 ได้: ' + (results[0]?.reason?.message || 'ไม่พบกล้อง'));
          }

          if (results[1]?.status === 'fulfilled') {
            testStream2 = results[1].value;
            testCam2.srcObject = testStream2;
            testCam2.style.display = 'block';
            testCam2.play().catch(err => console.error('Error playing testCam2:', err));
          } else {
            console.warn('⚠️ ไม่สามารถเปิดกล้องตัวที่ 2 ได้:', results[1]?.reason);
            testCam2.style.display = 'none';
            alert('ไม่สามารถเปิดกล้องตัวที่ 2 ได้: ' + (results[1]?.reason?.message || 'ไม่พบกล้อง'));
          }

          if (testStream1 || testStream2) {
            modal.style.display = 'flex';
          } else {
            throw new Error('ไม่สามารถเปิดกล้องใดๆ ได้');
          }
        })
        .catch(err => {
          console.error('เกิดข้อผิดพลาดในการทดสอบกล้อง:', err);
          alert('ไม่สามารถทดสอบกล้องได้: ' + err.message);
        });
    }

    function closeCameraTestModal() {
      const modal = document.getElementById('cameraTestModal');
      const testCam1 = document.getElementById('testCam1');
      const testCam2 = document.getElementById('testCam2');

      if (testStream1) {
        testStream1.getTracks().forEach(track => track.stop());
        testStream1 = null;
      }
      if (testStream2) {
        testStream2.getTracks().forEach(track => track.stop());
        testStream2 = null;
      }
      testCam1.srcObject = null;
      testCam2.srcObject = null;
      testCam1.style.display = 'none';
      testCam2.style.display = 'none';
      modal.style.display = 'none';
    }

    function openClipModal(url1, url2, timehit) {
      const modal = document.getElementById('clipModal');
      const video1 = document.getElementById('clipVideo1');
      const video2 = document.getElementById('clipVideo2');
      modal.style.display = 'flex';
      video1.style.display = url1 ? 'block' : 'none';
      video2.style.display = url2 ? 'block' : 'none';
      video1.pause();
      video2.pause();
      video1.removeAttribute('src');
      video2.removeAttribute('src');
      if (url1) video1.src = url1;
      if (url2) video2.src = url2;
      video1.load();
      video2.load();
      let seconds = hhmmssToSeconds(timehit) - 2;
      seconds = Math.max(0, seconds);
      function waitForVideoReady(video) {
        return new Promise(resolve => {
          if (!video.src) return resolve();
          video.onloadedmetadata = () => {
            video.currentTime = seconds;
            video.onseeked = () => resolve();
          };
          video.onerror = () => resolve();
        });
      }
      Promise.all([waitForVideoReady(video1), waitForVideoReady(video2)])
        .then(() => {
          if (url1) video1.play();
          if (url2) video2.play();
        })
        .catch(err => console.error('Error playing videos:', err));
    }

    function closeClipModal() {
      const modal = document.getElementById('clipModal');
      const video1 = document.getElementById('clipVideo1');
      const video2 = document.getElementById('clipVideo2');
      video1.pause();
      video2.pause();
      video1.removeAttribute('src');
      video2.removeAttribute('src');
      video1.load();
      video2.load();
      modal.style.display = 'none';
    }

    function hhmmssToSeconds(hhmmss) {
      const parts = hhmmss.split(':').map(Number);
      return parts.length === 3 ? parts[0] * 3600 + parts[1] * 60 + parts[2] : 0;
    }

    function formatSeconds(sec) {
      const h = Math.floor(sec / 3600);
      const m = Math.floor((sec % 3600) / 60);
      const s = Math.floor(sec % 60);
      return [h, m, s].map(n => String(n).padStart(2, '0')).join(':');
    }

    function renderFightRounds() {
      const container = document.getElementById('fightRoundsContainer');
      container.innerHTML = '';
      roundsData.forEach(round => {
        const table = document.createElement('table');
        table.className = 'fight-rounds';
        table.innerHTML = `
          <thead>
            <tr><th colspan="4">ข้อมูลการแข่งขันย้อนหลัง ยกที่ ${round.round - 1}</th></tr>
            <tr><th>เวลาที่โดน</th><th>นักชก</th><th>รายละเอียด</th><th>คลิปวิดีโอ</th></tr>
          </thead>
          <tbody>
            ${round.data.map(item => {
              const [value, timehit] = item.value.split('|');
              return `
                <tr>
                  <td>${timehit ? formatSeconds(timehit) : '-'}</td>
                  <td>${item.label}</td>
                  <td>${value}</td>
                  <td><button class="watch-btn" onclick="openClipModal('${round.videoUrl}', '${round.videoUrl2}', '${timehit ? formatSeconds(timehit) : '0:00:00'}')">ดูคลิป</button></td>
                </tr>
              `;
            }).join('')}
          </tbody>
        `;
        container.appendChild(table);
      });
    }

    // สรุปผลการแข่งขัน
    document.getElementById('btnSummary').addEventListener('click', () => {
      document.getElementById('summaryContent').innerHTML = 'กำลังโหลดข้อมูล...';
      fetch('/match/summary', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ schedulefightId: <%= schedulefightId %> })
      })
        .then(res => res.json())
        .then(data => {
          if (data.success) {
            let html = '';
            const f = data.fighters;
            let winCount = {};
            Object.keys(f).forEach(fid => winCount[fid] = 0);
            data.summaryByRound.forEach(r => {
              const round = r.round;
              const score1 = r.scores[Object.keys(f)[0]] || 0;
              const score2 = r.scores[Object.keys(f)[1]] || 0;
              const winnerId = r.winnerId;
              const winnerName = winnerId ? f[winnerId] : 'เสมอ';
              if (winnerId) winCount[winnerId]++;
              html += `<h3>ยกที่ ${round}</h3>`;
              html += `<p>${f[Object.keys(f)[0]]}: ${score1} คะแนน</p>`;
              html += `<p>${f[Object.keys(f)[1]]}: ${score2} คะแนน</p>`;
              html += `<p><strong>ผู้ชนะยกนี้: ${winnerName}</strong></p>`;
              html += `<hr>`;
            });
            const fids = Object.keys(f);
            const win1 = winCount[fids[0]];
            const win2 = winCount[fids[1]];
            let finalWinnerText = win1 > win2 ? `🏆 <strong>ผู้ชนะรวม: ${f[fids[0]]}</strong>` :
                                  win2 > win1 ? `🏆 <strong>ผู้ชนะรวม: ${f[fids[1]]}</strong>` :
                                  `🤝 <strong>ผลการแข่งขันเสมอ</strong>`;
            html += `<div style="margin-top:10px; font-size: 1.2em;">${finalWinnerText}</div>`;
            document.getElementById('summaryContent').innerHTML = html;
          } else {
            document.getElementById('summaryContent').innerHTML = `<p style="color:#ff4444;">เกิดข้อผิดพลาด: ${data.message}</p>`;
          }
          document.getElementById('summaryModal').style.display = 'flex';
        })
        .catch(err => {
          document.getElementById('summaryContent').innerHTML = `<p style="color:#ff4444;">เกิดข้อผิดพลาด: ${err.message}</p>`;
        });
    });

    socket.emit('requestConnectionStatus');
  </script>
</body>
</html>